{
  "rules": {
    "rooms": {
      "$room": {
        "uid_participants": {
          "$uid": {
            ".read": "$uid === auth.uid",
            ".write": "$uid === auth.uid"
          }
        }
      }
    },
    "users_private": {
      "$uid": {
        ".read": "$uid === auth.uid",
            ".write": "$uid === auth.uid"
      }
    },
    "users_public": {
      "$uid": {
        ".read": "$uid === auth.uid",
            ".write": "$uid === auth.uid"
      }
    }
  }
}