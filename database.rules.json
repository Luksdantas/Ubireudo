{
  "rules": {
    "rooms": {
      "$room": {
        "uid_participants": {
          "$uid": {
            ".read": "request.auth.uid == uid",
            ".write": "request.auth.uid == uid"
          }
        }
      }
    },
    "users_private": {
      "$uid": {
        ".read": "request.auth.uid == uid",
        ".write": "request.auth.uid == uid"
      }
    },
    "users_public": {
      "$uid": {
        ".read": "request.auth.uid == uid",
        ".write": "request.auth.uid == uid"
      }
    }
  }
}